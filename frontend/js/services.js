App.addTiRow=function(kind='regular',data={}){if(typeof kind==='object'){data=kind;kind='regular';}const annual=kind==='annual',tbody=annual?App.$('#tiAnnualItems'):App.$('#tiItems'),options=annual?App.tiAnnualOptions:App.tiOptions;const tr=document.createElement('tr');tr.dataset.tiKind=kind;tr.innerHTML=`<td><input class="unidade" placeholder="Unidade ou setor"></td><td><select class="serv">${options}</select></td><td><input class="tipo" readonly></td><td><input class="grandeza" readonly></td><td><input class="qtd" type="number" min="0" step="1" value="1"></td><td><input class="unit" type="number" min="0" step="0.01"></td><td class="money total">R$ 0,00</td><td><small class="billing"></small></td><td><button class="remove">×</button></td>`;tbody.appendChild(tr);['unidade','serv','qtd','unit'].forEach(k=>{if(data[k]!=null)tr.querySelector('.'+k).value=data[k];});if(data.manualUnit)tr.querySelector('.unit').dataset.manual='1';tr.querySelectorAll('input,select').forEach(e=>e.oninput=()=>App.recalcTiRow(tr));tr.querySelector('.unit').onchange=e=>e.target.dataset.manual='1';tr.querySelector('.remove').onclick=()=>{tr.remove();App.totals();};App.recalcTiRow(tr);};
App.recalcTiRow=function(tr){const annual=tr.dataset.tiKind==='annual',s=App.D.servicos[tr.querySelector('.serv').value],qtd=Number(tr.querySelector('.qtd').value||0),unit=tr.querySelector('.unit');if(!s){tr.querySelector('.tipo').value='';tr.querySelector('.grandeza').value='';tr.querySelector('.billing').textContent='';tr.dataset.monthly=0;tr.dataset.nonmonthly=0;tr.dataset.annual=0;tr.querySelector('.total').textContent=App.brl.format(0);App.totals();return;}tr.querySelector('.tipo').value=s.tipo||'';tr.querySelector('.grandeza').value=s.grandeza||'';tr.querySelector('.billing').textContent=s.faturamento||'';if(!unit.dataset.manual){const v=annual?(Number(s.precoAnual||0)||Number(s.precoMensal||0)*12):Number(s.precoMensal||s.precoAnual||0);unit.value=v.toFixed(2);}const total=qtd*Number(unit.value||0),monthly=!annual&&App.norm(s.faturamento)==='MENSAL';tr.dataset.monthly=monthly?total:0;tr.dataset.nonmonthly=!annual&&!monthly?total:0;tr.dataset.annual=annual?total:0;tr.querySelector('.total').textContent=App.brl.format(total);App.totals();};
